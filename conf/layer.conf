DISTRO = "ewong"

BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
           ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "ewong"
BBFILE_PATTERN_ewong = "^${LAYERDIR}/"
BBFILE_PRIORITY_ewong = "10"
